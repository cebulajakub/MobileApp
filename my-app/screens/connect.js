import React from "react";
import { View, Text, StyleSheet } from 'react-native'

const Connect = () => {

    return <View>
        <Text>Połączenie Wifi</Text>
    </View>
}

const styles = StyleSheet.create({})

export default Connect;